# 商品竞品分析 Agent｜GitHub Pages 演示版

此目录可以直接部署到 GitHub Pages。

1. 将本目录内的所有内容上传到 GitHub 仓库根目录；
2. 在仓库 **Settings → Pages** 中选择从 `main` 分支的 `/ (root)` 部署；
3. 等待 GitHub Pages 发布完成后，打开其提供的网址。

这是离线 Demo：不需要 API Key，也不会请求后端。页面中的空气炸锅资料用于课堂演示。

如需真实联网竞品分析，请使用完整项目包，在本地启动 FastAPI 后端并配置 API Key。
